stuff to install / run

ln ~/dotfiles/dotinputrc ~/.inputrc

i3
ln ~/dotfiles/i3.config ~/.config/i3/config

i3status
ln ~/dotfiles/i3status.config ~/.config/i3status/config

feh for background images
put images in 

vim
ctags
ln ~/dotfiles/dotvimrc ~/.vimrc

nvim
dein
ln ~/dotfiles/nviminit ~/.config/nvim/init.vim

mkdir ~/gitclones
github vim vundle
ln -s ~/gitclones/Vundle.vim ~/.vim/bundle/Vundle.vim

gsettings set org.gnome.desktop.lockdown disable-lock-screen 'true' 

