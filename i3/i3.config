#
#   an i3wm configuration. Carter Adams. 2018 - the end of time
#
#

# mod key is Windows
set $mod Mod4

# i3status bar. config in ~/dotfiles/i3/i3.status.config
bar {
        status_command i3status
        tray_output primary
        font pango:Dejavu Sans Mono 16
}

# swap escape and caps at launch (~/binfiles/swesc will do this too)
exec /usr/bin/setxkbmap -option "caps:swapescape"

# gnome network & bluetooth applets, gnominess, background image
#   background image random image from ~/.config/feh/images shown through feh
exec --no-startup-id nm-applet
exec --no-startup-id blueman-applet
exec --no-startup-id gnome-settings-daemon
exec echo "/home/$(whoami)/.config/feh/images/$(ls ~/.config/feh/images | sort -R | tail -n1)" | xargs feh --bg-scale

# small fonts for window titles
font pango:monospace 8

# mod + mouse to move floating windows
floating_modifier $mod

# mod + shift + q to kill a window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run -fn 'DejaVu Sans Mono-18'

# change focus with mod + arrows
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# mod + vim directions to move windows
bindsym $mod+Shift+h move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+j move up
bindsym $mod+Shift+l move right

# mod + shift + arrows to resize windows (imagine window in top-left)
bindsym $mod+Shift+Left resize shrink width 2 px or 2 ppt
bindsym $mod+Shift+Down resize grow height 2 px or 2 ppt
bindsym $mod+Shift+Up resize shrink height 2 px or 2 ppt
bindsym $mod+Shift+Right resize grow width 2 px or 2 ppt

# mod b, mod v makes next window split left-right / top-bottom
bindsym $mod+b split h
bindsym $mod+v split v

# mod f: go fullscreen, mod s: stack window group, mod e: change split style
bindsym $mod+f fullscreen toggle
bindsym $mod+s layout stacking
bindsym $mod+e layout toggle split

# mod+shift+space toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# mod+space change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# mod+a focus parent, mod+shift+a focus child
bindsym $mod+a focus parent
bindsym $mod+Shift+a focus child

# mod+number switch workspace
bindsym $mod+1 workspace 1
bindsym $mod+2 workspace 2
bindsym $mod+3 workspace 3
bindsym $mod+4 workspace 4
bindsym $mod+5 workspace 5
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# mod+shift+number move window to workspace
bindsym $mod+Shift+1 move container to workspace 1
bindsym $mod+Shift+2 move container to workspace 2
bindsym $mod+Shift+3 move container to workspace 3
bindsym $mod+Shift+4 move container to workspace 4
bindsym $mod+Shift+5 move container to workspace 5
bindsym $mod+Shift+6 move container to workspace 6
bindsym $mod+Shift+7 move container to workspace 7
bindsym $mod+Shift+8 move container to workspace 8
bindsym $mod+Shift+9 move container to workspace 9
bindsym $mod+Shift+0 move container to workspace 10


# mod+shift+r restart i3, mod+shift+e confirm logout, mod+ctrl+e no-confirm
#   logout, mod+ctrl+alt+del restart machine
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"
bindsym $mod+Ctrl+e exec "i3-msg exit"
bindsym $mod+Ctrl+Alt+Del exec "restart now"

# mod+t or mod+enter: terminal, mod+shift+t big terminal, mod+shift+e small
bindsym $mod+t exec gnome-terminal --window-with-profile="i3big" --hide-menubar
bindsym $mod+Return exec gnome-terminal --window-with-profile="i3big" --hide-menubar
bindsym $mod+Shift+t exec gnome-terminal --window-with-profile="i3presentation" --hide-menubar
bindsym $mod+Shift+Return exec gnome-terminal --window-with-profile="i3small" --hide-menubar

# mod+shift+v nvim on ~/wk, mod+shift+o open THELIST wordprocessor
#   mod+shift+n gvim of notefile for today (see ~/binfiles)
bindsym $mod+Shift+v exec gnome-terminal --window-with-profile="i3big" --hide-menubar -e "nvim ~/wk"
bindsym $mod+Shift+o exec libreoffice ~/etc/THELIST.odt
bindsym $mod+Shift+n exec notes

# mod+c for web browser
bindsym $mod+c exec firefox

bindsym $mod+Shift+s exec /home/redcartel/bin/bscreen laptop
bindsym $mod+Shift+b exec echo "/home/$(whoami)/.config/feh/images/$(ls ~/.config/feh/images | sort -R | tail -n1)" | xargs feh --bg-scale

